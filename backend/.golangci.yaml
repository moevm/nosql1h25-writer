linters:
  enable:
    - gocritic
    - govet
    - staticcheck
    - errcheck
    - errorlint
    - misspell
    - unused
    - whitespace
    - unparam
    - ineffassign
    - goconst
    - forcetypeassert
  disable:
    - typecheck
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - goimports
    - gofmt

run:
  timeout: 3m
  tests: true
